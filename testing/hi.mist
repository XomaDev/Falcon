func checkAge(n) = {
  if n < 18 {
    use "Hi kid!"
  } else {
    local name = "You just turned into an adult!"
    println(name)
    if n == 18 {
      use name
    } else {
      use "Hi, adult!"
    }
  }
}

func checkAge(n) = {
  if (n < 18) "Hi kid!"
  else {
    local name = "You just turned into an adult!"
    println(name)
    if n < 18 {
      "Child"
    } else {
      "Adult!"
    }
  }
}

func fooBar(n) = {
  local returning = ""
  if n < 18 {
    returning = "Child"
  } else {
    returning = "Adult"
  }
  returning
}