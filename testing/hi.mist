func generateStarTree(n) = {
  while (lineIndex <= n) {
    local starLine = ""
    local count = 1
    while (count <= lineIndex) {
      starLine = starLine _ "*" // Join operator for string concatenation
      count = count + 1
    }
  }
}
